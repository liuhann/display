import React, { useEffect, useState, useContext } from 'react'

export default ({
  element,
  width,
  height,
  zoom
}) => {
  useEffect(() => {
    // 加载并渲染组件
  })

  return (
      <div className='component' onClick={elementClicked} />
  )
}
